// Variáveis Locais




// Variáveis Globais
let alunos = 10;



// Operador para manipular variável
let idade = 31;

function main() {
    let nomeVariavel = 10;
    idade = (10 + 2) * 3;
    idade = 50 - 2

    // Variavel de escopo local
    let temperatura = 50;

    // Definição de precedencia
    temperatura = (50 + 2) * 3

    // Operador de incremento
    temperatura++;

    // Imprimindo
    console.log(`O valor de alunos é: ${alunos}`);
    console.log(`O valor de temperatura é: ${temperatura.toFixed(2)}`);

    // Operador de decremento;
    temperatura--;
}


main()